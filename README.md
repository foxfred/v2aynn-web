# v2aynn-web

低资源 Linux 设备（TV 盒子 / 路由器 / 树莓派）上的代理订阅管理器。基于 Xray-core，提供 Web 管理界面 + SOCKS5 / HTTP / 透明代理。

> **🤖 本项目由 AI 编程生成**，代码、文档、部署脚本均为人机协作的 AI 编程产物。
> **   如有大佬看上项目创意，拿去自行修改就好，本项目只提供基础使用，UI布局没什么讲究，有想法的拿去自己改。
---

## ✨ 功能特点

- **多订阅聚合**：支持多个订阅源合并管理，可配置订阅代理，支持周期自动刷新或关闭
- **协议支持**：vmess / vless / trojan / ss 四种主流协议自动解析
- **三种代理入口**：SOCKS5 (默认 10808) + HTTP (默认 10810) + 透明代理 (12345)，一键启停
- **智能分流**：国内域名/IP 直连（geosite:cn / geoip:cn），海外走代理；可切换全局模式
- **节点管理**：
  - 全部并发测速（每批 5 个），逐条实时显示结果，完成自动按延迟排序
  - 测速采用 TCP+TLS 真实握手验证（3 次采样优化为首次成功即返回，速度快、不易误判）
  - 超时节点以醒目红色标注，刷新后持久保留
  - 「更新订阅」按钮手动拉取订阅刷新列表
  - 延迟列独立显示，支持按延迟升/降序排序并**持久化保存**
  - 自动去重（相同 服务器:端口:协议 只保留一个）
  - 当前连接节点高亮
  - **手动添加节点**：支持通过表单手动填入节点信息
  - **粘贴导入节点**：支持粘贴 vless/vmess/trojan/ss 链接批量导入
  - **删除节点**：每行节点附带删除按钮
  - **真实下载测速**：通过代理隧道下载 2MB 文件计算实际 Mbps，测速 URL 可在设置中自定义
- **状态栏**：显示运行状态 + 当前节点名（长名自动省略，节点名缓存不受订阅后台刷新影响）
- **纯单页 Web 界面**：内联 CSS/JS，无任何外部依赖，移动端 / 电视浏览器友好
- **可靠性**：
  - 断电重启自动重连（带 5 次重试）
  - xray 进程崩溃自动拉起（最多 3 次，超过则放弃，防止无限重启）
  - 测试数据（ping/sort）随配置持久化，跨重启不丢失
  - 订阅拉取失败时保留旧节点，不整表清空
  - 手动添加/导入的节点不受订阅刷新影响，不会丢失
- **零 Go 外部依赖**：仅使用标准库，单二进制 ~5MB
- **无需 Docker**：`start.sh` 一键启动，自动安装 xray 与 geo 数据

---

## 🖥 技术栈

| 组件 | 说明 |
|------|------|
| Go | 单二进制 Web 服务（仅标准库） |
| Xray-core | 代理内核（VMess/VLESS/Trojan/Shadowsocks） |
| Alpine/Linux | arm64 / amd64 均可，低至 64MB 内存可用 |

---

## 🚀 快速开始

### 方式一：一键脚本（推荐）

```bash
# 1. 下载 xray + geo 数据（约 60MB，仅首次需要）
bash download-assets.sh        # 默认 arm64；amd64: bash download-assets.sh amd64

# 2. 启动
bash start.sh

# 3. 浏览器打开
#    http://<设备IP>:8000
```

### 方式二：systemd 服务（开机自启）

```ini
# /etc/systemd/system/v2aynn-web.service
[Unit]
Description=v2aynn-web proxy gateway
After=network.target

[Service]
Type=simple
Environment=DATA_DIR=/opt/v2aynn-web/data
ExecStart=/opt/v2aynn-web/v2aynn-web
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable --now v2aynn-web
```

### 方式三：本地编译

```bash
# 要求 Go 1.22+
GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -ldflags="-s -w" -o v2aynn-web ./cmd/server/
```

---

## 🔌 使用说明

1. **添加订阅**：在首页「订阅」栏填入名称和订阅 URL，点击添加
2. **测速排序**：点击「全部测速」测试所有节点延迟；点击延迟列标题按延迟排序（排序结果自动保存）
3. **切换节点**：点击节点即可切换，已连接节点有蓝框高亮
4. **透明代理**：可选，将局域网设备网关指向本机即可

---

## ⚙️ 端口一览

| 端口 | 用途 |
|------|------|
| 8000 | Web 管理界面 |
| 10808 | SOCKS5 代理 |
| 10810 | HTTP 代理 |
| 12345 | 透明代理（dokodemo-door） |

---

## 📦 项目结构

```
v2aynn-web/
├── cmd/server/main.go        # 入口：配置加载、订阅轮询、自动启动
├── internal/
│   ├── config/               # 配置结构、JSON 持久化、UUID
│   ├── subscription/         # 订阅拉取、协议解析、去重
│   ├── v2ray/                # xray 进程管理、配置生成
│   └── web/                  # HTTP API + 内嵌单页前端
├── download-assets.sh        # 下载 xray + geo 资源
├── start.sh                  # 一键启动
└── transparent.sh            # 透明代理开关脚本
```

---

## 📝 更新记录

### 2026-08-19
- **Xray JSON 订阅支持**：解析 BPB 面板等 `?app=xray` 格式的 JSON 数组订阅，自动提取 vless/vmess/trojan/ss 节点配置（含 ws/grpc/tcp/h2 流设置 + TLS/REALITY SNI 解析）
- **测速容错修复**：单个节点测速失败（网络抖动、ID 变更）不再中断整批测速；后端节点 ID 变更时返回超时而非错误，防止前端显示空白
- **订阅代理支持**：可在设置中配置订阅代理，解决盒子无法直接访问订阅源的问题
- **真实下载测速**：新增基于代理隧道的真实带宽测速（下载 2MB 文件计算 Mbps），URL 可在设置中自定义
- **测速 URL 可配置**：设置面板新增"测速目标URL"字段，默认 Cloudflare 2MB 文件

### 2026-08-21
- **界面居中布局**：`.app` 加 `max-width:1100px; margin:auto`，不再撑满浏览器宽度，大屏显示更紧凑
- **字体放大**：body 基础字号提升为 14px，各组件字号整体上调 2px，解决字太小问题
- **设置面板恢复**：顶栏新增"设置 ⚙"按钮，弹窗包含 SOCKS5端口/HTTP端口/Web监听地址/订阅刷新间隔/代理模式(智能/全局/直连)/全局订阅代理/测速URL
- **编辑订阅功能恢复**：后端新增 `POST /api/group/{id}/update`，支持修改订阅名称/URL/代理；前端分组项右侧"编辑"按钮，保存后清空旧节点并异步重新拉取订阅

### 2026-08-20
- **手动添加节点**：UI 新增表单，支持手动填入协议/服务器/端口/UUID/传输方式/TLS/SNI/路径等字段创建节点
- **粘贴导入节点**：支持粘贴 vless/vmess/trojan/ss:// 链接批量导入，每行一个
- **删除节点**：节点列表每行附带删除按钮，支持删除手动节点和订阅节点
- **订阅失败保护**：订阅拉取失败时保留该订阅原有节点，不再整表清空
- **手动节点持久化**：手动添加/导入的节点（SubID 为空）不受订阅刷新影响，不会在 Fetch 时被清除
- **防无限重启**：xray 异常退出后自动重启最多 3 次，连续失败（30s 内）则放弃，避免死循环
- **Trojan outbound 格式修复**：适配 xray 26.3.27，`password` 直接放在 `servers` 顶层而非嵌套在 `accounts` 中
- **切换节点防御**：切换前先验证节点存在，避免订阅刷新竞态下设置无效 ID

### 2026-08-16
- **测速算法重做**：回退易导致低配设备全超时的「临时起 xray 进程探测」，改用 TCP + TLS 真实握手验证；采样策略优化为首次成功即返回，全量测速显著提速
- **超时持久显示**：超时节点以红色"超时"标注，订阅刷新后不再退化为空白
- **逐条实时测速**：全部测速逐条显示进度与结果，完成后自动按延迟排序，无需手动刷新
- **新增「更新订阅」按钮**：手动拉取订阅并刷新节点列表
- **状态栏修复**：运行中显示当前节点名（缓存，不受后台订阅刷新影响），长名自动省略

### 2026-08-14
- **节点列表优化**：独立滚动区域（约 20 行），延迟单独列显示
- **排序持久化**：支持按延迟升/降序排序并保存到配置，重启后保留
- **断电重启自动重连**：开机自动启动代理（带 5 次重试）
- **节点去重**：按 `服务器:端口:协议` 去重，聚合订阅只保留唯一节点

---

## ⚖️ 免责声明

- 本项目仅用于学习与个人技术研究，请遵守所在地法律法规
- 含有的 Xray-core、pxy 数据等为第三方开源组件，版权归各自作者所有，请通过 `download-assets.sh` 从官方源获取
